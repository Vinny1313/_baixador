# Baixador Multimídia CLI

Um programa simples em Python para baixar músicas e vídeos.

## 🚀 Como usar em qualquer computador:
1. Instale o **Python**.
2. Instale as dependências: `pip install -r requirements.txt`.
3. Coloque o **ffmpeg.exe** e o **ffprobe.exe** na mesma pasta do script.
4. Execute: `python app.py`.
